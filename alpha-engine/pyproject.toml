[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "alpha-engine-quant"
version = "0.1.1"
description = "Portfolio Optimization & Risk Parity Engine"
readme = "README.md"
dependencies = ["numpy", "scipy"]
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "quant-signal-engine"
version = "0.1.1"
description = "Signal Processing and Statistical Alpha Generation"
readme = "README.md"
dependencies = ["numpy"]